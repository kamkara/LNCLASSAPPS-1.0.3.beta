<% flash.each do |key, value| %>
    <div class="alert alert-<%= key %>">
        <div class="flex">
            <ul>
                <%= value %>
            </ul>
            <a href="#" data-dismiss="alert" class="close">x</a>
        </div>
    </div>
<% end %>